import HeaderAuth from "./header-auth";

const Header = () => {
	return (
		<header className="flex justify-between p-4">
			<div />
			<div>
				<HeaderAuth />
			</div>
		</header>
	);
};

export default Header;
